name: deploy

on:
  push:
    branches: [master]

jobs:
  build: 
    runs-on: ubuntu-latest
    steps: 

      - name : Install Deps
        run : npm install .
      
      - name : deploy
        run : npm run deploy
